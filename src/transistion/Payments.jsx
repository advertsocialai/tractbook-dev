import PagePlaceholder from "../layouts/PagePlaceholder";

const Payments = () => (
    <PagePlaceholder
        title="Payments"
        description="Payments received and sent, with their settlement status."
    />
);

export default Payments;
