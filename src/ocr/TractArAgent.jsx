import PagePlaceholder from "../layouts/PagePlaceholder";

const TractArAgent = () => (
    <PagePlaceholder
        title="Tract AR Agent"
        description="Reads incoming receipts and invoices, then files them against the right customer."
    />
);

export default TractArAgent;
