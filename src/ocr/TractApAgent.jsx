import PagePlaceholder from "../layouts/PagePlaceholder";

const TractApAgent = () => (
    <PagePlaceholder
        title="Tract Ap Agent"
        description="Reads vendor bills and prepares them for approval and payment."
    />
);

export default TractApAgent;
